services:
  app:
    container_name: --PROJECT_NAME--_development_app
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    ports:
      - "${RAILS_PORT}:${RAILS_INTERNAL_PORT:-3000}"
    environment:
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_INTERNAL_PORT=${DB_INTERNAL_PORT:-5432}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${RAILS_INTERNAL_PORT:-3000}"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 30s
    user: app_user

  db:
    image: postgres:17.6-alpine
    container_name: --PROJECT_NAME--_development_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_DB=${POSTGRES_DB}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped
    ports:
      - "${DB_PORT}:${DB_INTERNAL_PORT:-5432}"

volumes:
  postgres_data:
    name: --PROJECT_NAME--_development_postgres_data
